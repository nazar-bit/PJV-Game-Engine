package cz.cvut.fel.pjv.entities;

/// EntityBehaviour is used to determine the behaviour of the entity
public enum EntityBehaviour {
    FOLLOW_THEN_IDLE,
    FOLLOW_THEN_GO_SPAWN,
    FOLLOW_THEN_WONDER,
    FOLLOW_FOREVER,
}
